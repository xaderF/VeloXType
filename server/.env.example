# ─── Backend Environment Variables (server/.env) ───
# Copy this file to server/.env and fill in real values.
# NEVER commit the real .env file to version control.

# ─── Required ──────────────────────────────────────
DATABASE_URL=postgresql://user:password@host:5432/veloxtype?sslmode=require
AUTH_SECRET=generate-a-64-char-random-string-here-minimum-32-characters-long

# ─── Production-only (enforced by env.ts) ──────────
# Comma-separated list of allowed frontend origins for CORS
CORS_ORIGIN=https://your-app.vercel.app

# Google OAuth (optional; required only if using Google OAuth login)
OAUTH_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ─── Optional ─────────────────────────────────────
# PORT is auto-set by Render; defaults to 4000 locally
# PORT=4000
NODE_ENV=production
DAILY_RESET_TIMEZONE=America/New_York

# Optional encryption keys (falls back to AUTH_SECRET if omitted, ≥32 chars)
# PII_ENCRYPTION_KEY=
# EMAIL_HASH_KEY=

# Optional: bootstrap admin users by username (comma-separated, lowercase recommended)
# ADMIN_USERNAMES=your_admin_username
